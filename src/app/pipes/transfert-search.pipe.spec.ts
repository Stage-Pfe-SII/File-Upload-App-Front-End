import { TransfertSearchPipe } from './transfert-search.pipe';

describe('TransfertSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new TransfertSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
