import { RemaningDaysPipe } from './remaning-days.pipe';

describe('RemaningDaysPipe', () => {
  it('create an instance', () => {
    const pipe = new RemaningDaysPipe();
    expect(pipe).toBeTruthy();
  });
});
